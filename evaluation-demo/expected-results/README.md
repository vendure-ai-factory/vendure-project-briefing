# Expected Results

The known-good reference returns two active catalog records in SKU order, with integer prices, URL-safe slugs, image counts, and no internal-only fields. The public verifier records this as `PASS` in the reference demonstration and as `BLOCK` when the unmodified starter is tested in acceptance mode.
